import './Filter.css'

import Product from "./Product";

const Filter = ({
  products,
  hotProducts,
  releaseProducts,
  promoProducts,
  addItem,
  showAlert,
  handleClickOpen,
}) => {
  return <div className="filter"></div>;
};

export default Filter